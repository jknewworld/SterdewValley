package com.P.Client.app;

import com.P.common.Message;
import com.P.Client.app.ServerConnectionThread;

import java.util.HashMap;

public class ClientCLIController {
    public static String processCommand(String command) {
//        HashMap<String, Object> body = new HashMap<>();
//        body.put("name", "sara");
//        body.put("age", 18);
//        if(command.equals("1")) {
//            Message response = ClientApp.getServerConnection().sendAndWaitForResponse(new Message(body, Message.MessageType.command), 500);
//            return response.getFromBody("result");
//        }
//        Message response = ClientApp.getServerConnection().sendAndWaitForResponse(new Message(body, Message.MessageType.update), 500);
//        return response.getFromBody("result");
        return null;
    }
}
